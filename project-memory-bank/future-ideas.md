# Future Ideas

## List of ideas
- Integrating Calendar providers (Google Calendar, Outlook) for automatic schedule importing.
- Mobile application layer built using React Native sharing the backend endpoint layer.
- An offline mode using indexedDB on the browser, syncs back when network reconnects.
- Local execution of models using Ollama or WebGPU inside browser for local execution agents.
